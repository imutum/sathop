// Vitest setup file (vitest.config.ts::setupFiles). Currently a stub —
// pinia was scaffolded by the original shadcn-vue template but never adopted
// (the app uses TanStack Query for server state). Keep the file so the
// vitest config keeps resolving; reach for it when global test plugins
// genuinely need to be installed.
export {};
