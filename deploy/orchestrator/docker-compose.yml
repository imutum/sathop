services:
  orchestrator:
    image: ${SATHOP_IMAGE:-ghcr.io/imutum/sathop}/orchestrator:${SATHOP_TAG:-latest}
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
